// webrd3 Prisma schema — datasource only.
// Real data models (User, Conversation, Message, ...) are added in T1.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
